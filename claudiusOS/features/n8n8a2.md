base is always 57; focus on odd numbers and prime;
When ever you talk about time you say base60(5) this then would mean
you speak in 5 minutes of base60 which is not the same equivelent of
base10(5) so main goal of this thing is integers only and primes;
prime example bucket; give a prime number base (57/or/59)
From here; bucket can be explained as following
example "garbage"=1  "can"=89 === garbage can in this context is 89 and 1 both is the
the same symbolic representation of the thing; existent or not;
they still experience half-life; now the data of it's material is unessecarry;
depending on context is it worth the energy to "investigate" === "x" or not?
garbargecan is 89+1=90 but also can be explained as 90-1=89 which is the same
as 89+1 but in a "different-context" now on top of this you can add odd numbers
to explain events; and in the end you add even numbers for the humans decision
here is a perfect system I'll show symbolic expression
based on context 89 is now a garbage(can) can be explain as 89+1 in base(10)
now in a AI-conversation we would mention based on data-poisioning and encryption
this are/is 89; would now in my head mean this is a garbage that's empty;
I have no idea if it makes sense; but tomorrow it could be another prime or odd-integear
example again; 89+1 how many garbage you can't really say that to here it means
"how many garbage cans' do we need to achive goal x or y" in this case
the goals of the 2D-space is undefined; but adding the integear as the act itself;
99+1 = 100; in [base(10)]
Now the lispOS will have a ??? what base are we talking about or Warn the human
that they are mixing base(x) base(y) or f(x+y) with 3D dimensional context again
89+1 = the garbage can is now definitive a varibale in our minds and todays context
(man I'm bored I sit at a train-station waiting for someon to send me money for vape)
((I'm creating this mean while haha))
how many 89+1(garbagecans) [89===would be how many garbages] I really need the
Japanese SOV katakana for this as inspiration for AlexNet 3.0 (I have no idea what
I'm saying just blurt out stuff data-poisioning is real)
how many 89(base10) do we need in this situation to fill all the (89+1)[base10] in our home
The main purpose of our symbolic lisp is daily based on context and relationships;
Through this each day words meaning will rotate; and the base10 too; a calcualtion of a trajectory
will be shown in 5 different ways meaning its harder for someone to "prepare" direction or
trajectory of something even a persons choice or words;
